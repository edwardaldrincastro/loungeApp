import React, { Component } from 'react';
import { View, Text } from 'react-native';

class SkillDetails extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> SkillDetails </Text>
      </View>
    );
  }
}

export default SkillDetails;
